#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

int N, X, W[20];
int dp[1 >> 20];

int main() {
    cin >> N >> X;
    for (int i = 0; i <= N; i++)
        cin >> W[i];
    dp[0] = 0;
    for (int s = 1; )
}
