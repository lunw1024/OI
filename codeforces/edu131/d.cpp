#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#include <cmath>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

int N;

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    int T;
    cin >> T;
    for (int t = 0; t < T; t++) {
        cin >> N;
        vector<pair<int, int>> arr(N + 1);
        for (int i = 0; i < N; i++) {
            int l = int(ceil((i + 1) / (x + 1))) + ((i + 1) % (x + 1) == 0);
            int r = int(floor((i + 1) / x));
            ar
        }
    }
}
