#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

int N;

int main() {
    // easy combinatorics
    // case 1: 1 source or 1 sink
    //     all permutations
    // case 2: >=2 sources and sinks
    //     sources to one side, sinks to the other, ok points be anywhere
    // O(n)
}