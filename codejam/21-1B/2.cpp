#include <iostream>
#include <algorithm>
#include <vector>
#include <map>
using namespace std;
using ll = long long;

int main() {
    int T;
    cin >> T;
    for (int t = 0; t < T; t++) {
        int N, A, B;
        cin >> N >> A >> B;
        vector<int> U;
        for (int i = 0; i < N; i++) {
            int x;
            cin >> x;
            U.push_back(x);
        }
        
    }
}
